Program Example2; 
Const M = 6;
Var a,b,c, arr[10];
PROCEDURE gcd(a; b; var d; var e);  
	Begin    
	   While a <> b do
		if a > b then a := a - b
		else b := b - a;	
	  d := b;
	  b := 123456789
	End;
	
PROCEDURE ff;  
	Begin    
	   While a <> b do
		if a > b then a := a - b
		else b := b - a;	
	  
	  b := 123456789;
	End;

Begin  
(* Call ff; *)
Call gcd(1 * 3 + 5, a, a, c);
5;
c := M * 2


a := gcd;
Call gcd(a,  c);
M := 5;
 (*  Call Readln(a);	Call Readln(b);
	Call gcd(a,b,c; 
	Call Writeln(c); *)
End.

Begin a + 