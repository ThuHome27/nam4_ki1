#include <stdio.h>


int main(int argc, char const *argv[])
{
	// FILE *fp;
	// fp = fopen("text2.c", "r");
	// char c;
	// int count = fread(&c, sizeof(c), 1, fp);
	// while (count == 1) {
	// 	printf("%c\n", c);
	// 	count = fread(&c, sizeof(c), 1, fp);		
	// }
	// printf("%d\n", count);
	// printf("%c\n", c);
	int f = 2023456789;
	printf("%d\n", f);
	return 0;
}
